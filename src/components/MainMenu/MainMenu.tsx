import React from 'react';

import { StMenuContainer } from './MainMenu.styled';

const MainMenu: React.FC = () => (
  <StMenuContainer>
    <div className="width-container">Menu</div>
  </StMenuContainer>
);

export default MainMenu;
